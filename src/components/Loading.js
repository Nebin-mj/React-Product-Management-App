import React from "react";

const Loading = () => {
   return (
      <>
         <span className="spinner-grow text-primary" role="status"></span>
         <span>Loading...</span>
      </>
   );
};

export default Loading;
